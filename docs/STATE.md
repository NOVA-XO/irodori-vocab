# Одоогийн байдал — Мартчихлаа

**Амьд хаяг:** https://nova-xo.github.io/irodori-vocab/
**Хувилбар:** `2026-09-22q` (`index.html` дахь `?v=` ба `sw.js` дахь `VERSION` хоёр
ижил байх ёстой — зөрвөл офлайн кэш хуучин файл өгнө)
**Хамгийн сүүлд шалгасан:** 2026-09-21 — `test_ui.js` 227/227 · `test_attack.js` 24/24 · `test_answers.js` цэвэр

Энэ файл нь **амьд лавлагаа**. Огноотой шинэ файл үүсгэхгүй — үүнийг засна.

---

## 1. Өгөгдлийн одоогийн тоо

| Файл | Бичлэг | Лавлах (`ref`) | Тайлбар |
|---|---:|---:|---|
| `data/vocab.json` | 1232 | 196 | Irodori 入門 + гараар оруулсан 50 (`lesson: 0`) |
| `data/vocab-el1.json` | 1192 | 195 | Irodori 初級1 |
| `data/vocab-el2.json` | 1786 | 434 | Irodori 初級2 |
| `data/vocab-n5.json` | 2077 | 0 | N5 · 50 хичээл · 97%-д жишээ өгүүлбэртэй |
| **Үг нийт** | **6287** | | |
| `data/kanji.json` | 1187 | — | Монгол утга **1187/1187**, JLPT түвшинтэй 979 |
| `data/kana.json` | 107 | — | 五十音 46 · 濁音 25 · 拗音 36 |
| `data/audio.json` | 10828 | — | 149.1 МБ, 96 kbps моно 24 кГц |
| `data/exam-starter.json` | 100 | — | Харилцааны асуулт, 入門 L1–L18, өөрөө үнэлэх. 漢字/かな/ふりがな гурван хэлбэртэй |

Дууны задаргаа (2026-09-11 дахин тоолов): 入門 1232 · 初級1 1192 ·
初級2 1786 · N5 үг 2077 · **N5 жишээ өгүүлбэр 2013** · `音読み` 1184 ·
`訓読み` 1037 · кана 107 = **10628**, дээр нь 2026-09-21-нд нэмсэн
шалгалтын **200** (`EXQ-` 100 асуулт + `EXA-` 100 загвар хариулт, 4.5 МБ)
= **10828**. Бүгд дискэн дээр байгаа (дутуу 0, илүү 0).
`data/audio.json`-ы `ids` нь эрх мэдэлтэй жагсаалт.

Нийтлэгдэх хэмжээ (`git ls-files` + шинэ дуу): **160.2 МБ = 1 ГБ хязгаарын 16.0%**.
Ажлын хавтас 3.9 ГБ харагддаг нь `tools/_vv/` (VOICEVOX хөдөлгүүр) —
`.gitignore`-д байгаа тул нийтлэгдэхгүй.

---

## 2. Орчны урхи — бодитоор тохиолдсон

Дараах бүхэн **энэ төсөл дээр үнэхээр тохиолдсон**. Ижил урхинд хоёр дахин
орохгүйн тулд бичив.

### 2.1 Windows консол — кирилл/япон хэвлэвэл унана
Python-аас `print()`-ээр кирилл эсвэл кана хэвлэвэл `UnicodeEncodeError:
'charmap' codec` гарч скрипт **тасалдана** (cp1252). Хэдэн ч удаа тохиолдсон.

**Шийдэл:** скриптийн гаралтыг зөвхөн **латинаар** бич. Кирилл/япон
харах шаардлагатай бол файлд бичээд тусад нь `cat`-аар үзнэ.

### 2.2 Bash heredoc урвуу зураасыг эвддэг
`bash -c` дотор heredoc-оор Python бичихэд `\n`, `\\u`, `\+`, `\s` зэрэг
**эвдэрдэг**. Үүнээс болж `test_answers.js`-д жинхэнэ шинэ мөр орж
`SyntaxError: Invalid or unexpected token` гарсан. Мөн `SyntaxWarning:
invalid escape sequence` гарч байсан.

**Шийдэл:** урвуу зураас агуулсан ямар ч зүйлд (regex, `\n`, Windows зам)
Bash-ийн оронд **Write/Edit хэрэгсэл** ашиглана.

### 2.3 Supabase публик түлхүүр RLS-ийг тойрдоггүй — АЛДАА ЧИМЭЭГҮЙ
`sb_publishable_…` түлхүүрээр REST-ээр хүснэгт уншихад **алдаа гардаггүй**,
зүгээр л `[]` буцна. GitHub Action «амжилттай» дуусаад «шинэ санал алга» гэж
бичсэн — 30 минут шалтгаан хайлаа.

**Шийдэл:** `feedback-to-issues.yml` одоо түлхүүрийн **хэлбэрийг** шалгаж
(`eyJ` / `sb_secret_` / `sb_publishable_`) публик түлхүүр дээр тодорхой
мессежээр зогсдог. Түлхүүр өөрөө логт хэзээ ч гарахгүй.

### 2.4 GitHub Secrets — хуулахад зай/шинэ мөр ордог
`SUPABASE_URL` дотор санамсаргүй зай орсноос `curl: (3) URL rejected:
Malformed input to a URL function` гарсан. Логт нууц нь `***` тул шалтгаан
харагдахгүй.

**Шийдэл:** workflow одоо `tr -d '[:space:]'`-ээр цэвэрлэж, төгсгөлийн `/`-ыг
хасна.

### 2.5 `set -e` + `[ тест ] && команд`
```bash
[ "$COUNT" -eq 0 ] && exit 0      # ← COUNT != 0 үед бүхэл мөр 1 буцаана
```
`set -e`-тэй бол ажил тэр дор нь **тасалдана**. Хоёр газар байсан.

**Шийдэл:** бүтэн `if … then … fi` бич.

### 2.6 GitHub Pages нь Git LFS-ийг ЗАДАЛДАГГҮЙ
LFS-д тавьсан дууны файлын оронд **заагч текст** файлыг өгдөг. Дуу огт
гарахгүй. Энэ нь GitHub-ийн албан ёсоор хүлээн зөвшөөрсөн хязгаарлалт.

**Шийдэл:** дууг **энгийн git файлаар** хадгална. 72.5 МБ бол ямар ч
асуудалгүй (1 ГБ хязгаартай).

### 2.7 GitHub Pages кэш — `max-age=600`
Файл сольсон ч 10 минут хуучин хувилбар үйлчилнэ.

**Шийдэл:** `index.html` дахь `?v=…` ба `sw.js` дахь `VERSION` **хоёуланг**
хамт сольж push хийнэ. Хоёр газрыг мартвал офлайн кэш хуучин файл өгнө.

### 2.8 Headless Chrome — медиа + `--virtual-time-budget` гацдаг
`--autoplay-policy=no-user-gesture-required`-тай хамт урт budget өгвөл
скриншот **бичигдэхгүй, процесс гацна** (120 сек timeout).

**Шийдэл:** `--headless=new --mute-audio --virtual-time-budget=4000..6000`,
мөн тестийн хуудсанд `HTMLMediaElement.prototype.play`-ыг хиймлээр
`Promise.resolve()` болгож дарж бичнэ.

### 2.9 iframe-ээр аппыг удирдах — хоёр урхи
1. **Cross-origin:** удирдах хуудсыг `file://`-ээс нээвэл `localhost`-ын
   iframe-ийн `document` рүү хандаж чадахгүй, `try/catch` дотор чимээгүй
   унана. → Удирдах хуудсыг **ижил origin**-аас (`http://localhost:8731/…`)
   үйлчлүүлнэ.
2. **`let` нь `window` дээр гардаггүй:** `w.ALL` гэж шалгавал хэзээ ч
   бэлэн болохгүй. → **DOM**-оор шалгана (ж: `#n-vocab` нь `…` биш болсон эсэх).

### 2.10 `[hidden]` ажиллахгүй байсан
`.pane{display:grid}` гэсэн зохиогчийн дүрэм браузерын `[hidden]{display:none}`-ыг
дардаг (author > user-agent). Бүх дэлгэц зэрэг харагдаж байсан.

**Шийдэл:** `app.css`-д `[hidden]{display:none !important}`.

### 2.11 VOICEVOX «AquesTalk風記法» нь `ー`-г хүлээж авдаггүй
`ミャ'ンマー` → `UNKNOWN_TEXT: ー`. Уртатгалыг **эгшгээр** бичих ёстой:
`ミャ'ンマア`.

**Шийдэл:** `expand_choon()` нь өмнөх морагийн эгшгээр солино. Моралын
тоо хадгалагдах ёстой — эс тэгвэл унах байрлалын индекс гажна.

### 2.14 Монгол Ү/Ө нь Windows-ийн фонтуудад БАЙХГҮЙ
**Serif:** `Georgia` · `Times New Roman` · `Palatino Linotype` ·
`Cambria` · `Constantia`.
**Монспэйс:** `Consolas` · `Cascadia Mono` · `Courier New`.

Бүгдэд нь Ү (U+04AE) ба Ө (U+04E8) дутуу. Хөтөч тэдгээрийг өөр фонтоос
орлуулдаг тул нэг үгийн дунд хоёр өөр загварын үсэг холилдоно —
монспэйс дээр бүр эгнээ ч эвдэрнэ.

**Шийдэл:** `Noto Sans Mono`-г өөрсдөө нийлүүлнэ (11 КБ,
`tools/build_fonts.py`).

**2026-09-21 шинэчлэл:** аппын БҮХ бичиг `Noto Sans JP` болсон.
Тэр нь Ү/Ө-г **агуулна** (`U+04AE-04AF`, `U+04E8-04E9` — Google
Fonts-оос `text=`-ээр шалгаж баталсан), тиймээс `--ui`/`--disp` ч
системийн фонт руу унахаа больсон. Дэд фонтыг үүсгэхдээ япон
тэмдэгт дээр латин+кириллийг ЗААВАЛ нэмнэ (`jp + lc`) — эс тэгвэл
монгол текст чимээгүйхэн Segoe UI рүү буцна.

### 2.15 Google Fonts `text=` нь URL урт бол ЧИМЭЭГҮЙ үл тоогддог
1568 тэмдэгт өгөхөд (URL 14 КБ) огтолсон НЭГ файлын оронд бүтэн фонтыг
**124 хэсгээр** буцаав. Алдаа өгөхгүй — зөвхөн үр дүн нь өөр.

**Шийдэл:** 300 тэмдэгтийн багцаар хүсэлт тавьж, буцах `url()`-ийн тоог
шалгана; 1-ээс олон бол зогсоно.

### 2.21 `background-position`-ын анимаци нь бүтэн дэлгэцийг дахин зурна
GPU-д зөөгдөхгүй тул кадр болгонд repaint болно. Утсан дээр гурван ийм
давхарга зэрэг ажиллаж байхад мэдэгдэхүйц гацдаг.

**Шийдэл:** `transform: translate3d()`. Дэвсгэрийг хавтгайгаар давтаж,
элементийг ЯГ нэг хавтгайн хэмжээгээр зөөвөл заадасгүй давтагдана.
Налуу зураасны хувьд (0,0)→(w,h) шугам нь хоёр тэнхлэгт таардаг.

**Бас:** `vmax` нэгжээр өгсөн давхарга нь утсан дээр дэлгэцээс 3.5 дахин
том талбай зурдаг. `%` нь `#bg`-ийн (дэлгэцийн) хэмжээтэй холбогддог тул
найдвартай. `backdrop-filter`-ийг НЭГ-ХОЁР гадаргуугаас хэтрүүлэхгүй.

### 2.23 Data URI доторх `#` — НЭГ л удаа кодлоно
SVG доторх `url(#gradientId)` лавлагааг `%23` гэж урьдчилан бичвэл
`urllib.parse.quote()` түүнийг дахин кодлож `%2523` болгоно. Градиент
олдохгүй, элемент огт зурагдахгүй — АЛДАА өгөхгүй, зүгээр л хоосон.
Түүхий SVG-д `#` гэж бичээд кодлолтыг нэг удаа хийлгэ.

### 2.24 Шуугиан нь ТОД ИРМЭГТЭЙ хэлбэр өгч чадахгүй
`feTurbulence` нь үргэлж бүдэг, тархай толбо гаргана. Анимегийн үүл,
логотип, дүрс зэрэг ТОДОРХОЙ силуэт шаардсан зүйлийг шуугианаар хийж
болохгүй — зурах хэрэгтэй. Давхарласан тойрог нь нэг силуэт болж
нэгддэг тул овоохой хэлбэрт тохиромжтой.

**Хэмжээний урхи:** үүлний суурийг нэг том тойргоор өгвөл радиус нь
өргөнөөс хамаарч аварга болно. Өндрөөр хэмжигдэх жижиг тойргийн эгнээ
ашигла.

### 2.22 `#bg`-ийн pseudo нь хүүхдүүдээс ДООГУУР зурагдана
`#bg` нь `z-index:0`-тэй тул давхаргын хүрээ үүсгэнэ. `::before` нь
`z-index` авахгүй бол `i` хүүхдүүдийн ДООГУУР орно. Борооны зураасыг
дуслын давхарга бүтэн дэлгэцээр угааж байсан шалтгаан энэ.

### 2.20 Маск нь бүтэц өгдөг, ГЭРЭЛ өгдөггүй
`feTurbulence`-ийг зөвхөн `mask-image` болгож ашиглавал ямар хэсэг
харагдахыг л заана — гадаргуу хавтгай хэвээр үлдэж, «будсан» мэт
харагдана. Гурван удаа тааруулсан ч материал бодитой болоогүй.

**Шийдэл:** `feDiffuseLighting` / `feSpecularLighting` нь оролтын
**альфа сувгийг өндрийн зураг** болгон уншиж гэрэл тусгана. Металл, ус
зэрэг эзэлхүүнтэй материалд ЗААВАЛ энэ хэрэгтэй.

Хоёр анхаарах зүйл:
· Гэрлийн өнгө нь дэвсгэрийн өнгөтэй ойрхон бол юу ч харагдахгүй.
  Усны дуслын биеийг БАРААН болгосны дараа л дусал мэт харагдав.
· Гаралтыг data URI болгож хадгалбал хөтөч филтер тооцохгүй —
  нэг удаа растержина.

### 2.19 Хөдөлгөөнийг тодорхой агшинд ЗОГСООЖ зураг ав
`--virtual-time-budget` нь WAAPI нислэгийг санамсаргүй агшинд барина —
дунд нь ч, дууссаны дараа ч байж болно. Тогтвортой зураг авахын тулд:

    d.getAnimations().forEach(a => { a.currentTime = 300; a.pause(); });

CSS шилжилтийн хувьд `*{transition:none !important}` (2.18-ыг үз).

### 2.18 Theme сольсны дараа шууд зураг авахыг БОЛИХ
`button` дээр `transition: background-color .18s, color .18s` байгаа тул
`data-theme`-ийг сольсны дараа авсан зураг **шилжилтийн дунд** байдлыг
барина: товч, гарчиг хар харагдаж, «токен ажиллахгүй» гэсэн ХУУРАМЧ
дүгнэлт төрүүлнэ. `--virtual-time-budget` энэ шилжилтийг найдвартай
дуусгадаггүй.

**Шийдэл:** зураг авах эсвэл хэмжихийн өмнө iframe дотор
`*,*::before,*::after{transition:none !important}` хэвлэнэ. Эргэлзвэл
`getComputedStyle`-аар токеныг ШУУД хэмж — зургаар бүү дүгнэ.

### 2.17 Headless Chrome-ийн цонх 500px-ээс НАРИЙН болдоггүй
`--window-size=430,1000` гэж өгсөн ч `innerWidth` нь **500** хэвээр
(Windows дээр шалгав: 380 → 500, 430 → 500, 500 → 500). Зураг нь
хүссэн өргөнөөр хайчлагдана — үр дүнд нь **хуудас баруун талаасаа
тасарсан мэт харагдана**. Утсан дээр халилт гарлаа гэж андуурч
болзошгүй; бодитоор халилт БАЙХГҮЙ байв (`scrollWidth 415 = clientWidth 415`).

**Шийдэл:** утасны өргөнийг шалгахдаа дээд түвшний цонх биш,
**хүссэн өргөнтэй `iframe`**-ийг ≥500px цонх дотор ашиглана.
`--force-device-scale-factor=2` нь энэ хайчлалыг илүү ил болгодог тул
эргэлзвэл түүгээр давхар шалга.

### 2.16 `--blink-settings=preferredColorScheme=2` нь headless-ийг унагадаг
`GPU process exited unexpectedly: exit_code=-1073741819`. Зураг бичигдэхгүй.

**Шийдэл:** Харанхуйг шалгахад флагийг ОГТ өгөхгүй — headless анхдагчаар
харанхуй горимд ажилладаг. Цайваныг шалгахад л `preferredColorScheme=1`.

### 2.33 Анон түлхүүрээр put_progress мөр ҮЕРЛҮҮЛЖ болно
Синк код 7.88e17 орон зайтай тул IDOR брутфорс боломжгүй. ГЭХДЭЭ анон
түлхүүрээр put_progress-ийг САНАМСАРГҮЙ кодоор дуудаж хязгааргүй мөр
үүсгэж (тус бүр 4 МБ) free-tier 500 МБ санг дүүргэж болно.

**Шийдэл (2026-09-11d):** `ping`-тэй ижил — `progress.created_at` нэмж,
цагт 200 ШИНЭ код. Байгаа кодын шинэчлэлт хамаарахгүй, цаг тутам сэргэдэг.

**Трэйд-офф:** хатуу нийт тааз тавибал довтлогч түүнд хүрч ШИНЭ
хэрэглэгчийг мөнхөд түгжинэ. Цагийн хязгаар нь сэргэдэг тул түгжихгүй,
харин цагт 200-аас олон жинхэнэ шинэ хэрэглэгч зэрэг ирвэл (маш ховор)
хойшилно.

### 2.34 Пентест нь БОДИТ хүснэгтэд хог мөр үлдээдэг
`add_feedback`-ийг шалгахад бодит feedback хүснэгтэд мөр орж, 30 минут
тутмын GitHub Action тэдгээрийг Issue болгодог. put_progress-ийн шалгалт
progress-д мөр үлдээнэ. Анон түлхүүрт DELETE эрх байхгүй тул SQL Editor-оор
цэвэрлэнэ. Шалгалтын өмнө цэвэрлэх SQL бэлдэж, шалгасны ДАРАА шууд ажиллуул.

### 2.29 `esc()` нь ХАШИЛТЫГ орлуулдаггүй — атрибутад ХЭРЭГЛЭХГҮЙ
`esc()` нь зөвхөн `&` `<` `>` гурвыг орлуулна. Атрибутын дотор тавивал
`id="A" onmouseover="…"` гэсэн утга тэндээс ГАРЧ жинхэнэ атрибут
үүсгэнэ. Браузер дотор баталсан (2026-09-11):

    data-a="A" onmouseover="window.__x=1" y=""   ← DOM-д атрибут болов

**Дүрэм:** элементийн ДОТОР `esc()`, АТРИБУТАД `escA()`.

**Тест бичихэд:** HTML МӨРӨӨР бүү шалга. `onmouseover=&quot;` гэсэн
текст нь аюулгүй, `onmouseover="…"` гэсэн атрибут нь аюултай — хоёул
мөрөнд `onmouseover=` гэж харагдана. `el.attributes`-ыг үз.

### 2.30 `jsonb_set()`-ийг ДАВТАЖ дуудвал O(n²)
`jsonb_set` нь баримтыг бүхэлд нь ДАХИН угсардаг. Түлхүүр бүрд дуудвал
хэмжээ өсөхөд квадратаар удаашрана. Хэмжив: **2000 карт → 13.17 сек**,
тэр хугацаанд `for update` түгжээг барина.

**Шийдэл:** ялах бичлэгийг НЭГ `jsonb_object_agg`-аар цуглуулаад `||`-оор
нийлүүлнэ (баруун тал давамгайлдаг). O(n log n).

### 2.31 `(v ->> 'x')::int` нь хорлонтой jsonb дээр ГҮЙЛГЭЭГ унагана
`{"n":"abc"}` ирвэл cast алдаа өгч бүх гүйлгээ унана. `jnum(jsonb)`
туслах функц ашиглана: `jsonb_typeof(v) = 'number'` эсэхийг шалгана.

### 2.48 `toKana()` нь ЛАТИНД зориулагдсан — япон оролтыг хаядаг
`toKana(src)` нь `[^a-z\-']` бүхнийг устгадаг тул `ねこ` → `''`.
`checkTyped()` нь ЗӨВХӨН түүгээр дамжуулж байсан учир **япон гараар
зөв бичсэн хариултыг «буруу» гэж тэмдэглэдэг байв**. Браузерт баталсан:
`ねこ`, `ネコ`, `おちゃ`, `せんせい` — бүгд татгалзаж, зөвхөн `neko`
хэлбэр л ажиллаж байв.

**Шийдэл:** `set.has(normKana(raw))`-ыг НЭМЭВ. `answerSet` нь
`normKana`-гаар хэвийн болгож хадгалдаг тул оролтыг мөн тэгж хэвийн
болгоход катакана→хирагана хөрвөж, ханз хэвээр дамжина.

**Дүрэм:** оролт хүлээн авдаг функцийг ГАДААД гараар туршиж үз —
латин туршилт л бичвэл ийм алдаа мөнхөд үлдэнэ.

### 2.49 Гүйлтийн тэмдэг нь ӨГӨГДӨЛД ч хэрэгтэй
Синк нь сүлжээнд нислэгт байхад хэрэглэгч явцаа устгавал, буцаж ирсэн
үүлний өгөгдөл устгасныг ЭРГҮҮЛЖ тавьдаг байв. `syncNow()` нь
`syncCode` солигдсоныг шалгадаг боловч УСТГАЛ нь кодыг өөрчилдөггүй
тул тэр хамгаалалт барихгүй.

**Шийдэл:** `progGen` тоолуур — устгал бүрд ахиулж, синк буцахдаа
шалгана (§2.36-ийн тэмдгийг ӨГӨГДӨЛД хэрэглэсэн хувилбар).

### 2.50 Кана «сонсоод таах» — ИЖИЛ дуутай хос
Япон хэлэнд `お/を · じ/ぢ · ず/づ · じゃ/ぢゃ · じゅ/ぢゅ · じょ/ぢょ`
нь ИЖИЛ дуудагдана — дууны файл нь ч байт хүртэл ижил (шалгав).
Тэднийг сонголтод зэрэг гаргавал сурагч чихээрээ ялгаж чадахгүй:
«о» сонсоод お дарахад апп «буруу» гэнэ. Хэрэглэгч үүнийг мэдээлсэн.

`buildChoices()`-ийн `usable` нь ижил БИЧИГТЭЙГ л хасдаг байсан;
`klisten` горимд ижил ДУУТАЙГ ч хасах болов.

**Дүрэм:** дуугаар асуудаг дасгалд сандруулагчийг ДУУГААР шүү,
бичгээр биш.

### 2.47 «Явц» нь ГУРВАН хадгалалтад тархсан
«Явцыг устгах» нь зөвхөн `irodori.progress.v1`-ийг цэвэрлэж байв.
Гэтэл хэрэглэгчийн «явц» гэж ойлгодог зүйл гурван газар:

| Түлхүүр | Юу | Цэвэрлэх ёстой уу |
|---|---|---|
| `irodori.progress.v1` | карт бүрийн хайрцаг, давталт | ✔ |
| `irodori.days.v1` | ӨНӨӨДРИЙН хариулт, дараалсан өдөр | ✔ (мартагдсан) |
| `settings.last` | «Үргэлжлүүлэх» товчны дасгал | ✔ (мартагдсан) |
| `irodori.settings.v1` | бичиг, зорилт, дуу | ✘ тохиргоо |
| `irodori.scheme.v1` | цайвар/бараан | ✘ тохиргоо |
| `irodori.dev.v1` | төхөөрөмжийн id | ✘ устгавал нэг хүн хоёр болж тоологдоно |

Хоёрыг мартсан тул устгасны дараа ч нүүрэн дээр «Өнөөдрийн зорилт
21/20», «5 өдөр дараалан» хэвээр үлдэж, хэрэглэгч устгаагүй гэж
ойлгож байв.

**Дүрэм:** «бүгдийг устга» гэсэн үйлдэл бичихдээ хадгалалтын түлхүүр
БҮРИЙГ нэг бүрчлэн тоол — тархсан төлөв нь чимээгүй үлддэг. Тестэд
НҮҮРЭН ДЭЭРХ бодит тоог шалга, зөвхөн `progress`-ийг биш.

### 2.46 «Устгах» товч нь ҮҮЛНИЙ хуулбарыг цэвэрлэх ЁСТОЙ
«Явцыг устгах» нь локал `localStorage`-ийг зөв цэвэрлэдэг байсан
(браузерт шалгав: 5 → 0). Гэтэл `syncNow()` нь
`mergeProgress(progress, remote)` хийдэг тул хоосон локал + бүтэн үүл
= БҮГД БУЦНА. Мөн `put_progress` нь СЕРВЕР дээр ч уусгадаг
(`v_old || winners`) тул хоосон өгөгдөл түлхэх нь цэвэрлэдэггүй.
Синктэй хэрэглэгчийн хувьд товч нь үнэхээр ажиллахгүй байв.

**Шийдэл:** `wipe_progress(p_code)` RPC (`SUPABASE.sql`). Хэрэв сервер
дээр байхгүй бол (хуучин SQL) синкийг ЛОКАЛААР салгаж, өгөгдөл буцаж
ирэхгүй болгоод хэрэглэгчид ил хэлнэ.

**Мөн — ХАРИУ МЭДЭГДЭЛ:** товч нь Профайл дээр байдаг, харин
`refreshStats()`/`refreshHome()` нь ӨӨР дэлгэцүүдийг шинэчилдэг.
Хэрэглэгч «тийм» гэж дараад ЮУ Ч харахгүй тул товч эвдэрсэн гэж
ойлгоно. Үйлдэл нь харагдахгүй бол ЗААВАЛ бичгээр хэл.

### 2.45 `show()` дотор `screen` нь ЭРТ дарагддаг
Буцах товчийг холбохдоо `show()`-ийн дотор
`if (name !== screen) navTo(name);` гэж бичсэн нь хэзээ ч ажиллаагүй —
`screen = name;` нь тэр мөрөөс ДЭЭР байдаг тул нөхцөл үргэлж худал.
Түүх огт бичигдэхгүй, буцах товч сайтаас гаргасаар байв.

**Дүрэм:** `show()` дотор өмнөх дэлгэцийг хэрэглэх бол функцийн ЭХЭНД
`const wasScreen = screen;` гэж тогтоо. Алдаа нь ЧИМЭЭГҮЙ — тест нь
`history.back()`-ийг БОДИТООР дуудаж байж л барина.

### 2.44 `AudioContext`-ыг УРЬДЧИЛАН үүсгэвэл гар утсан дээр гацна
Хэрэглэгчийн үйлдлээс ӨМНӨ `new AudioContext()` хийвэл `suspended`
төлөвт үүсч, дараа нь `resume()` дуудахгүй бол дуу гарахгүй. Тиймээс
контекстыг ЭХНИЙ хэрэгцээнд нь (хариулт өгөх үед — тэр нь өөрөө
үйлдэл) үүсгэнэ. Дээрээс нь таб далд болох үед хөтөч түүнийг
унтраадаг тул дуудалт бүрд `state === 'suspended'` эсэхийг шалгана.

**Мөн:** осциллятор эхлэх/дуусахад дууны далайцыг огцом тасалбал
чанга «клик» сонсогдоно — богино дугтуй (`exponentialRampToValueAtTime`)
заавал тавина. `exponential`-д ТЭГ утга өгч болохгүй, `0.0001`-ээс
эхэлнэ.

**iOS:** `navigator.vibrate` нь Safari-д БАЙХГҮЙ. Чичиргээг заавал
`if (navigator.vibrate)` гэж хамгаална, мөн хэрэглэгчид ил хэл —
эс тэгвэл «ажиллахгүй байна» гэсэн санал ирнэ.

### 2.43 Шилжилттэй элементийг `hidden`-ээс ШУУД нээж болохгүй
`hidden` нь `display:none`. Түүнийг аваад тэр дор нь `transform`-ыг
эцсийн утгад нь тавибал браузер завсрын төлөвийг зурахгүй — элемент
шууд байрандаа **үсэрч** гарч ирнэ, шилжилт огт харагдахгүй.

**Дүрэм:** хоёр алхам. `hidden`-ийг авч, `requestAnimationFrame`
дотор `open` ангиа нэм. Хаахдаа эсрэгээр: `open`-ыг аваад, шилжилтийн
хугацаа өнгөрсний ДАРАА `hidden`. `transitionend` дээр найдаж
болохгүй — `prefers-reduced-motion` үед тэр үйл явдал хэзээ ч ирэхгүй
тул элемент мөнхөд нээлттэй үлдэнэ. Таймер найдвартай.

**Мөн:** `display:flex` нь браузерын `[hidden]{display:none}`-ийг
дардаг (§2.10-тай ижил урхи) тул `[hidden]{display:none !important}`
шаардлагатай.

### 2.41 Тестийн үр дүнг `grep`-ээр шалгавал exit код АЛДАГДАНА
`node tools/test_ui.js | grep -E "FAIL|цэвэр:" && git commit …` гэж
бичсэн нь тест УНАСАН хэвээр commit хийж, түлхэв. Шалтгаан нь
шугамын exit код нь СҮҮЛИЙН тушаалынх — grep нь мөр олсон тул 0
буцаасан.

**Дүрэм:** түлхэхийн өмнө exit кодыг ТУСАД нь ав:
```bash
node tools/test_ui.js > /dev/null 2>&1; echo "exit=$?"
```

### 2.42 Нуугдсан элементийн хэмжээс нь БҮГД тэг
«Картууд давхцаагүй» гэсэн тестэд `getBoundingClientRect()`-ийг
дараалан хасахад `-639` гэсэн утга гарав. Давхцал байгаагүй —
JLPT-ийн карт нь `JLPT_ON=false` үед нуугддаг бөгөөд нуугдсан
элементийн `top`/`bottom` нь 0 байдаг тул тооцоог гажуудуулсан.

**Дүрэм:** байрлал хэмжихдээ `height > 0` гэж ШҮҮ.

**Мөн:** CDP-д илгээх загварчилсан мөр (`c.ev(`…`)`) дотор
тайлбар бичихдээ ХАЖУУ ХАШИЛТ хэрэглэвэл мөр тасарч
`SyntaxError: missing ) after argument list` гарна.

### 2.40 Гараар дарах ба системийн сонголт — ХОЁР сонгогч хэрэгтэй
Цайвар/бараан нь `prefers-color-scheme`-ээр системийг дагаж байсан.
Дээр нь гар удирдлага нэмэхэд `@media`-гийн дотор байгаа барааныг
хэрэглэгч «цайвар» гэж сонгоход ДАРЖ БИЧИХ хэрэгтэй болно. CSS нь
media query-г атрибутаар дардаггүй тул:

```css
:root{ /* цайвар */ }
@media (prefers-color-scheme: dark){
  :root:not([data-theme="light"]){ /* бараан */ }
}
:root[data-theme="dark"]{ /* ДАХИН бараан */ }
```

`:not([data-theme="light"])`-гүй бол систем бараан үед «цайвар»
сонгосон нь ажиллахгүй. CSS блок хуваалцдаггүй тул барааны токенууд
ДАВХАРДАНА — нэгийг нь засвал нөгөөг нь ч зас. `test_ui.js` §4 нь
системийг БАРААН болгож эмуляцлаад «цайвар» сонгоход үнэхээр цайвар
болж байгааг шалгадаг: энэ нь хоёр сонгогчийн шууд нотолгоо.

**Мөн:** `meta[name=theme-color]`-ийн `media` атрибут нь ЗӨВХӨН
системийн сонголтыг мэднэ. Гараар дарсныг мэдэхгүй тул хөтөч буруу
өнгө үзүүлнэ — JS нь хоёуланг нь бодит өнгөөр дүүргэх ёстой.

### 2.39 Токенжсэн CSS нь БҮХЭЛ загварыг солихыг хямд болгодог
Таван загвар, чимэглэлийг хасаад цайвар/бараан руу шилжихэд өнгөний
ажил нь `:root`-ийн **25 мөр** л байв — бусад 640 мөр CSS хөндөгдөөгүй.
Учир нь өнгө, радиус, зай бүр токеноор дамждаг байсан.

**Дүрэм:** CSS-д хатуу өнгө БИТГИЙ бич. Нэг удаа хатуу бичсэн өнгө нь
бараан горимд чимээгүйхэн эвдэрнэ — тэр газар нь зөвхөн нүдээр л
олдоно. Шинэ дүрэм нэмэхдээ `var(--…)` ашиглаж байгаа эсэхээ шалга.

**Мөн:** `display:block`-ыг `inline-block` болгоход доорх ах дүү
элемент нь блокоос инлайн болж ХАЖУУД нь бичигдэв («дараалсан өдөр»
гэсэн шошго тооныхоо хажууд гарсан). Зөвхөн дэвсгэрийг нь агуулгаар
хумих гэж байвал `width:fit-content` хэрэглэ, `inline-block` биш.

### 2.38 ふりがな зэрэгцүүлэлт — латин үсэг ХОЁР янзаар уншигдана
`q` (ханз) ба `qKana` (кана) хоёроос аль кана аль ханзанд харьяалагдахыг
олоход ханзгүй хэсгүүдийг ЗАНГУУ болгодог. Гурван зүйл барив
(`tools/build_exam.py`, 2026-09-21):

1. **Зангуу нь уншлагын ДОТОР тааралдана.** `日本に` → `にほんに`:
   эхний `に` нь `にほん`-ы дотор байгаа тул 日本-д хоосон уншлага
   оногдоно. → ухрах хайлт, ханз бүрд дор хаяж 1 кана.
2. **Араб тоо нь уншлагатай.** `25歳` → `にじゅうごさい`. Тоог дараах
   ханзнаасаа САЛГАЖ болохгүй: тоолуур өмнөх тооноос хамаарч
   өөрчлөгддөг (`3階` さんがい · `5階` ごかい). Мөн `1,000円` нь 7
   тэмдэгт боловч уншлага нь 4 кана тул «тэмдэгт бүрд 1 кана» гэсэн
   доод хязгаарыг ЗӨВХӨН ханзаар тоолно.
3. **Латин үсэг хоёрдмол.** `M` → `エム`, `Tシャツ` → `ティーシャツ`
   (уншигдана), гэтэл `ABC会社` → `ABCがいしゃ` (хэвээрээ). Хоёуланг
   нь туршиж, **зангуу гэж үзсэн хувилбар ТҮРҮҮЛЖ** шалгана — латин
   нь канан хэлбэрт хэвээрээ байвал уншигдахгүй гэсэн үг.

**Хоёр хэрэгжүүлэлт.** `build_exam.py` (Python) нь шалгалтын 200 мөрийг
урьдчилж бэлддэг; `app.js` дахь `rubyPairs()` нь ижил алгоритмын JS
ХУУЛБАР — 6287 үгийг урьдчилж хадгалбал өгөгдөл дэмий таргална.
Хоёр хэрэгжүүлэлт САЛЖ явбал үгийн сан дээр буруу ふりがな гарна,
тиймээс `test_ui.js` §12 нь JS-ийн үр дүнг Python-ы бэлдсэн хостой
**тулгадаг** (165 уншлагатай мөр, зөрүү 0).

**Үгийн санд 97.4%.** 4523 ханзтай үгийн 4406 нь цор ганц шийдтэй.
Үлдсэн 117-д ふりがな ХАРУУЛАХГҮЙ — хуучин кана мөр хэвээр үлдэнэ.
Ихэнх уналт нь ӨГӨГДЛИЙН алдаа (кана талбарт англи үг орсон, эхний
мора алга г.м.) — §4-ийн нээлттэй асуудлыг үз.

**Дүрэм:** зэрэгцүүлэлтийг build үед хийж, БҮХ шийдийг тоол. Нэгээс
олон бол эргэлзээтэй тул build-ыг УНАГА — апп руу буруу хуваалт
гаргахаас таслан зогсооно. Одоогоор 198/198 мөр цор ганц шийдтэй.

### 2.37 Тестийн ТОГТМОЛ профайл нь service worker-ийн хуучин кэшийг барина
`data/audio.json` нь `sw.js`-ийн CORE precache-д байдаг. Дуу дахин
үүсгэсний дараа `test_ui.js` «асуулт бүрд VOICEVOX бичлэг бий» гэсэн
2 шалгуур унав. Апп нь **бодитоор зөв байсан**: ШИНЭ профайлаар
CDP-ээр шалгахад `AUDIO_IDS.size = 10828`, `hasAudio("EXQ-S04-04") =
true`. Уналтын шалтгаан нь тестийн `--user-data-dir` тогтмол байсан
тул service worker хуучин 10628-тай `audio.json`-г өгч байсан явдал.

**Дүрэм:** тест бүр ШИНЭ профайлаар ажиллана —
`os.tmpdir() + '\\irodori-uitest-' + Date.now()`. Хоёулаа (`test_ui.js`,
`test_attack.js`) ийм болсон.

**Мөн:** `data/*.json`-ыг дахин үүсгэх бүрд **хувилбар заавал ахина**
(`index.html` дахь `?v=` 7 газар + `sw.js` дахь `VERSION`). Код
өөрчлөгдөөгүй ч кэш задлагч хэрэгтэй — эс тэгвэл хуучин хэрэглэгчид
хуучин `audio.json`-той үлдэж, шинэ бичлэгүүд «байхгүй» болно.

### 2.36 ASYNC + setTimeout — ГҮЙЛТИЙН ТЭМДЭГГҮЙ бол өөр гүйлтэд нөлөөлнө
Хойшлуулсан дуудлага (fetch-ийн `.then`, `setTimeout`) нь хэрэглэгч аль
хэдийн өөр зүйл хийж байхад ажилладаг. Браузерт ХОЁУЛАНГ нь давтав
(2026-09-21):

· Шалгалтын хариултын 240мс таймер нь ШИНЭ шалгалтын `exIdx`-ийг
  ахиулж эхний асуултыг алгасав (`exIdx=1`, «2 / 10», `exLog` хоосон).
· `loadExam()`-ийн fetch нислэг дунд хэрэглэгч нүүр рүү гарсан ч
  `.then` дэх `show('exam')` эргүүлж татав.
· Ярианы таниулт нь `stop()`-ийн дараа ч эцсийн үр дүнгээ хүргэдэг тул
  ДАРААГИЙН картыг хариулж болзошгүй байв.

**Дүрэм:** async/хойшлуулсан ажил бүрт ГҮЙЛТИЙН ТЭМДЭГ. Эхлэхдээ
`const run = ++xRun;`, буцаж ирэхдээ `if (run !== xRun) return;`.
Гарах/дахин эхлэхэд тэмдгийг ахиулна (`exAbort()`, `stopRecog()`).
Ярианы таниулт цуцлахад `stop()` БИШ `abort()` — `stop()` үр дүнг
хүргэсээр байдаг.

**Мөн:** товчийг `disabled` болгосон бол амлалт ямар ч замаар дуусахад
сэргээ; өлгөөтэй fetch-д `withTimeout()` тавь — эс тэгвэл товч мөнхөд
идэвхгүй үлдэнэ.

Регресс: `tools/test_ui.js` §9b «№5», «№6».

### 2.35 Astra (codex exec) — FOREGROUND + reasoning=low
gpt-6-astra-г `nohup ... &` background-аар ажиллуулбал streaming хариултын
БИЕ алдагдаж, зөвхөн промтын echo үлддэг ("codex" хэсэг, "tokens used"
footer гарахгүй). Мөн xhigh reasoning + том даалгавар нь хоосон хариу өгдөг.
Оношилгоо: энгийн `exec ... "Reply hello"` foreground-д зөв ажилладаг.

**Дүрэм:** Astra-г FOREGROUND-д (background биш) `-c model_reasoning_effort=low`
ба ЖИЖИГ, нэг үр дүнтэй даалгавраар ажиллуул. Гаралт нь CRLF тул fenced
блок задлахад `
?
` хэрэглэ. 2026-09-15-нд StudyCard.jsx/css амжилттай
гаргаж, browser-д баталсан (5/5).

### 2.32 Codex нь ДОВТОЛГООНЫ аудитаас татгалздаг
«RED-TEAM», «attacker», «exploit» гэсэн үг бүхий даалгаварт OpenAI-ийн
шүүлтүүр `possible cybersecurity risk` гэж хариулж, «Trusted Access for
Cyber» хөтөлбөрт бүртгүүлэхийг шаардана. Codex файлуудыг уншиж дуусаад
ЭЦСИЙН хариу өгөх мөчид татгалздаг тул токен дэмий зарцуулагдана.

**Дүгнэлт:** аюулгүй байдлын довтолгооны шалгалтыг Codex-д битгий
даалга. Бүтцийн аудит (2026-09-11-ний 21 олдвор) нь асуудалгүй өнгөрсөн.

### 2.27 `refreshHome()` нь `pool`-ыг СОЛИДОГ — дасгал дундуур дуудвал унана
`refreshHome()` эхний мөрөндөө `rebuildPool()` дуудаж `pool`-ыг ИДЭВХТЭЙ
НОМЫН үг болгодог. Гэтэл `finish()` нь `refreshHome()` дуудаж дуусдаг.
Улмаар ханзны дасгал дууссаны дараа `deck` нь `'kanji'` хэвээр, `pool`
нь ҮГ болчихсон байна.

«Алдаагаа давтах» дарвал `buildChoices()` нь тэр `pool`-оос сандруулагч
сонгож `simKey()` рүү өгнө → `it.on[0]` → **TypeError, дасгал унана**.

Браузер дотор давтав (2026-09-11): `deck='kanji'` · `pool` 79 ханзнаас
98 ҮГ болов · `Cannot read properties of undefined (reading '0')`.

**Засав (`2026-09-11b`):** `refreshHome()` нь `pool`-ыг ОГТ хөдөлгөхгүй
болов; нүүрэн дэх тоог тусад нь тооцно. «Давтах» нь `pool`-ыг өөрийн
сангаас сэргээнэ. Регресс тест: `tools/test_ui.js` §9 «№4».

**Тест бичихэд бас хамаатай:** `deck`-ийг гараар тавиад `pool`-ыг
орхивол яг ижил уналт гарна — энэ нь аппын алдаа БИШ, тестийн алдаа.
Тиймээс дасгалыг үргэлж `startSession()` / `startKanji()` / `startKana()`
гэсэн БОДИТ замаар эхлүүлнэ (`tools/test_ui.js` §5 тийм).

### 2.28 Chrome-ийг автоматжуулахад гадаад сан ХЭРЭГГҮЙ
puppeteer, playwright аль нь ч суугаагүй. Node 24-д `WebSocket` дотоод
байдаг тул CDP-тэй шууд ярьж болно (`tools/test_ui.js`, ~100 мөр):

1. `chrome.exe --headless=new --remote-debugging-port=9222`
2. `PUT http://127.0.0.1:9222/json/new?<url>` → `webSocketDebuggerUrl`
3. `Runtime.evaluate` + `awaitPromise:true` — хуудсан дотор JS ажиллуулна

`Runtime.exceptionThrown` ба `Runtime.consoleAPICalled` дээр сонсвол
чимээгүй унасан алдааг барина. Зурагнаас ХАМААГҮЙ найдвартай: зураг нь
«харагдаж байна» гэдгийг л хэлдэг, «зөв ажиллаж байна» гэдгийг хэлдэггүй.

### 2.25 Аппыг нээхэд илгээдэг ping нь ХЭРЭГЛЭЭГ хэмжиж чадахгүй
`pingUsage()` нь өдөрт нэг удаа, **аппыг нээх агшинд** явдаг. Тэр
агшинд хэрэглэгч хараахан юу ч давтаагүй тул явцын тоо нь 0. Хэрэв
тухайн хүн дахин хэзээ ч нээхгүй бол түүний бүх хөдөлмөр бүртгэлгүй
үлдэнэ — «нээгээд хаясан» мэт харагдана.

**Шийдэл (2026-09-11a):** `ping(..., p_bump boolean)`. Дасгалаас
гармагц `p_bump: false`-оор дахин дуудна — тоо шинэчлэгдэнэ, нээлтийн
тоо хөдлөхгүй. Холбоос нь `show()` дотор (`screen === 'study'` →
өөр дэлгэц), учир нь `finish()` нь `show('done')`-оор төгсдөг тул
дуусгасан ба дундуур гарсан хоёул нэг цэгт таарна.

### 2.26 PostgREST — функцийн ГАРЫН ҮСЭГ өөрчлөхөд `replace` хүрэлцэхгүй
`create or replace function ping(text,int,int,int,boolean)` нь
`ping(text)`-ыг СОЛИХГҮЙ, зэрэгцээ overload үүсгэнэ. Дараа нь
PostgREST `{p_dev}` дуудлагыг аль нь рүү чиглүүлэхээ мэдэхгүй болж
алдаа буцаана.

**Шийдэл:** шинийг үүсгэхийн өмнө `drop function if exists
public.ping(text);`. Шинэ аргументуудыг `default null` болговол
кэшлэгдсэн хуучин клиент асуудалгүй ажиллана.

**Дараалал чухал:** SQL-ийг ЭХЛЭЭД RUN хийж, дараа нь клиентээ push.
Эсрэгээр хийвэл `ping` чимээгүй бүтэлгүйтэж (`.catch(() => {})`) тоо
цуглуулах зогсоно.

### 2.13 Хэрэглээний тоонд ХӨГЖҮҮЛЭЛТИЙН тест бүртгэгддэг байв
Headless Chrome бүр удаад **шинэ түр профайл** үүсгэдэг → `localStorage`
хоосон → шинэ `devId` → `devices` хүснэгтэд «шинэ хэрэглэгч». Тест нь
`localhost`-оос ажилладаг ч `sync-config.js` нь репод байдаг тул `ping`
жинхэнэ Supabase руу очно. Тоо чимээгүй хөөрөгддөг.

**Шийдэл (2026-09-10y):** `pingUsage()` дотор `isDevHost()` — `localhost`,
`127.0.0.1`, `[::1]`, `*.local`, `file:` дээр бүртгэхгүй.

**Анхаар:** 2026-09-10 04:17 — 2026-09-10y хүртэлх хугацаанд цугларсан
тоонд миний тест ОРСОН. `usage_days()`-ын цаг тутмын задаргаагаар л
ялгаж болно.

### 2.12 `pypdf` координат буруу
Span-уудын `x`, `y` нь 0 гарч байсан. → **PyMuPDF** (`pymupdf`) ашиглана.

---

## 3. Шалгаад ЦЭВЭР гэж батлагдсан хэсгүүд

| Огноо | Юу | Хэрхэн | Үр дүн |
|---|---|---|---|
| 2026-09-10 | Хариулт шалгагч, **дөрвөн сан** | `node tools/test_answers.js` | exit **0**. Кана: 入門 99.8% · 初級1 **100%** · 初級2 99.9% · N5 **100%** |
| 2026-09-10 | `toKana` хөрвүүлэгч | 12 тохиолдол | 12/12 |
| 2026-09-10 | `checkTyped` — хаалт, は/わ, ō-ийн 2 бичлэг | 10 тохиолдол | 10/10 |
| 2026-09-10 | Supabase RLS — `progress` | анон түлхүүрээр `INSERT`/`UPDATE`/`DELETE`/`SELECT` | INSERT `42501`, бусад 0 мөр, SELECT `[]` |
| 2026-09-10 | Supabase RLS — `feedback`, `devices` | анон түлхүүрээр шууд `SELECT` | хоёулаа `[]` |
| 2026-09-10 | `add_feedback` спам хязгаар | 4 удаа дараалан | 4 дэх нь `too many` |
| 2026-09-10 | `add_feedback` төрлийн шалгалт | `p_kind='hack'` | `bad kind` |
| 2026-09-10 | `ping` / `usage_stats` | RPC дуудалт | `204`, тоо зөв нэмэгдэв |
| 2026-09-10 | Дуу үүсгэлт | 6538 файл | **0 алдаа**, 72.5 МБ |
| 2026-09-10 | Дуу — амьд хаяг | `curl` 4 файл | бүгд `200`, хэмжээ зөв |
| 2026-09-10 | 初級1 задлалт | `extract_vocab.py el1` | **0 асуудал** / 1192 |
| 2026-09-10 | Өргөлтийн тэмдэглэгээ → VOICEVOX | 12 тохиолдол | бүгд зөв (`ミャ'ンマア` [1], `ハジメマ'シテ` [4]) |
| 2026-09-10 | Хэвтээ халилт (390px) | `scrollWidth` vs `clientWidth` | 390 = 390, **халилтгүй** |
| 2026-09-10 | Сонголтод япон үг холилдох | 60 карт гүйлгэв | **0 холилдол** |
| 2026-09-10 | **Санал хүсэлт → Issue урсгал** | 2 санал илгээж Action ажиллуулав | `feedback 5 → #1`, `6 → #2`, шошго зөв |
| 2026-09-10 | Issue давхардал | Action-ыг дахин ажиллуулав | `хүлээгдэж буй: 0`, нийт Issue 2 хэвээр |
| 2026-09-10 | N5 задлалт | `extract_n5.py` 2 .docx | 2077 үг / 50 хичээл, 9 асуудал |
| 2026-09-10 | N5-ийн утга давхардал | Зэрэгцээ мөр харьцуулав | 5 нэр дэвшигч → 2 жинхэнэ алдаа зассан, 3 нь ижил утгатай үг |
| 2026-09-10 | **Хэрэглээний тоо — миний тестийг ялгав** | `usage_days()` цаг тутмын задаргааг session-ий Chrome дуудалтын цагтай тулгав | 19 төхөөрөмжөөс **4 нь миний тест** (10:36×2, 10:37, 10:38) → жинхэнэ **15** |
| 2026-09-10 | Явц — бүх хэсэг | Хиймэл явц суулгаж headless-ээр 2 таб зурав | 6 хэсэг бүгд зурагдав; дээд мөрийн «үзсэн 44» = зурааснуудын нийлбэртэй тохирлоо |
| 2026-09-10 | **和 дизайн — 12 дэлгэц** | headless-ээр цайвар ба харанхуй хоёуланд зурав | нүүр · Irodori · JLPT · кана · үгийн карт · ханзны карт · сонголт · бичих · явц · санал хүсэлт · профайл — бүгд зөв |
| 2026-09-10 | Фонтын кирилл хамрах хүрээ | 7 serif фонтыг 34px-ээр зурж харьцуулав | Georgia/Times/Palatino/Cambria/Constantia — Ү,Ө дутуу; Noto Serif — бүрэн |
| 2026-09-10 | Хэвтээ халилт — 和 дизайны дараа | 375px ба 415px дээр `scrollWidth` vs `clientWidth`, DOM-ийн бүх элементийг тоолов | 375=375 · 415=415 · халисан элемент **0** |
| 2026-09-10 | Амьд фонт | 7 `.woff2` + `OFL.txt` GitHub Pages дээрээс | бүгд `200`, хэмжээ нь локалтай тэнцүү |
| 2026-09-10 | **Зургаан загвар** | 5 загварт `--btn-bg` `--ink` `--accent` ба `.primary` `.bigcard b`-ийн бодит өнгийг `getComputedStyle`-аар хэмжив | бүгд зөв; нүүр ба дасгалын дэлгэцийг 4 загварт зурав |
| 2026-09-10 | **3D цагираг ба сугалалт** | Irodori-гоос флашкарт эхлүүлж DOM-ыг хянав | цагираг 14 карт · нисдэг карт үүсэв (`え？`) · дасгалын үг ЯГ тэр (`え？`) · нислэгийн үед карт нуугдсан · дараа нь цэвэрлэгдсэн · дасгал хэвийн |
| 2026-09-10 | **Таван загвар** | 5 загварт нүүр · сонголт · дасгал · явц · профайлыг зурав | бүгд ялгаатай, зөв. Дэвсгэрийн зураглал 4 өөр аргаар ажиллав |
| 2026-09-10 | **Фрактал бүтэц бүх загварт** | 蒼·機·漫·雨 дөрвийг зурж харьцуулав | ус · үрсэн ган · цаасны ширхэг · шилэн дусал — бүгд байгалийн харагдав |
| 2026-09-10 | **機·漫·雨 дахин зохиолт** | Судалгааны дараа 2x нягтралаар зурж шалгав | 機 хайрцаггүй, үс шугам, аюулгүйн зураас · 漫 хүрээний шатлал + kakeami + тоны screentone · 雨 линз товойлттой дусал |
| 2026-09-10 | **Гүйцэтгэлийн аудит** | `background-position` анимаци, давхаргын хэмжээ, `backdrop-filter`-ийн тоог шалгав | 3 бүтэн дэлгэцийн repaint → 0 · давхарга 3.5x → 1.5x · `backdrop-filter` 6 → 2 |
| 2026-09-10 | Монспэйсийн кирилл | Consolas · Cascadia · Courier · DejaVu · ui-mono-г 30px-ээр зэрэгцүүлэв | бүгдэд нь Ү,Ө дутуу → Noto Sans Mono нийлүүлэв |

### Сөрөг үр дүн — дахин ухах шаардлагагүй
- **JMdict-д монгол хэл БАЙХГҮЙ.** Япон→монгол толь бичгийн бэлэн өгөгдөл
  олдохгүй. JLPT-ийн ~12,000 үгийг гараар орчуулахаас өөр арга алга.
- **JLPT нь ханзны ч, үгийн ч албан ёсны жагсаалт нийтэлдэггүй** (2010 оноос).
  N5–N2 нь албан бус. Албан ёсны нь зөвхөн 文科省-ийн `学年別漢字配当表`
  (өгөгдөлд `g` талбар).
- **Irodori 初級1/2-ын ことばリスト-д ромажи багана байхгүй.** Тиймээс
  тэдгээрт ромажи тест хамаарахгүй — кана тестээр л шалгана.

---

## 4. Нээлттэй асуудал

| # | Асуудал | Хэнд | Тайлбар |
|---|---|---|---|
| 1 | `data/REVIEW.md` — 29 / 1232 (2.4%) | гараар | 入門-ий задлалтын үлдэгдэл. Ихэнх нь хуурамч сэрэмжлүүлэг (англи тайлбар, тоо). |
| 2 | `L10-010 片付けて（ください）` — ромажи нь `↓` | гараар | Тестийн цорын нэг татгалзсан бичлэг. `tools/fixes.json`-д засвар нэмнэ. |
| 3 | `data/REVIEW-n5.md` — 68 / 2077 (3.3%) | гараар | Бараг бүгд нь «жишээ өгүүлбэр алга» (мэндчилгээний хэллэгүүд) — жинхэнэ дутагдал биш. |
| 4 | `data/REVIEW-el2.md` — 16 / 1786 (0.9%) | гараар | `E2-16-088` уншлагагүй; `E2-14-055` кана дутуу таслагдсан. |
| 5 | Бүлгийн гарчиг хоёр мөр болвол дутуу таслагдана | код | ж: `だれが何を持って行` (『きますか？』 дутуу). y-ээр нэгтгэх цонх 4pt тул хоёр ЗУРАГЛАЛЫН мөрийг нийлүүлдэггүй. `section` нь аппад хэрэглэгддэггүй тул чухал биш. |
| 6 | JLPT **N4–N2**-ын үг хэрэгжээгүй | шийдвэр | N5 нь бэлэн (2077). N4–N2-д толь бичиг байхгүй тул гараар орчуулах шаардлагатай. |
| 7 | Хэрэглээний **асуулт хариулт** хэрэгжээгүй | шийдвэр | `Can-do checklist` (албан ёсны монгол) араг яс болгох. Номын яриаг үг үсэгчлэн хуулбал нээлттэй репод эрхийн эрсдэлтэй. |
| 8 | Утасны дуу батлагдаагүй | эзэн | `speechSynthesis`-ээс бэлдсэн mp3 болсон тул шийдэгдсэн байх ёстой, гэхдээ хэрэглэгч батлаагүй. |
| 10 | **117 үг ふりがな-гүй** — ихэнх нь ӨГӨГДЛИЙН алдаа | гараар | ふりがな зэрэгцүүлэлт 4523 ханзтай үгийн 4406-г (97.4%) шийдэв. Үлдсэн 117-г шалгахад бодит эвдрэл илэрлээ: `E1-06-041 見える / みえる to see` (кана талбарт АНГЛИ үг), `E1-15-043 下がる / る （ねつが さがる）` ба `E1-15-070 下げる` (эхний мора `さが`/`さげ` алга), `E2-03-074 以上で… / じょうで…` (`い` алга), `E1-15-057/059/060 大正／大 · 平成／平 · 令和／令` (ханзан тал гажсан), `E1-15-068` (хаагаагүй хаалт). Үлдсэн нь олон уншлагатай (`～本 / ～ほん／ぽん／ぼん`) эсвэл кана нь давхардсан (`飲み物 / のみもの／のみもの`) — эдгээр нь жинхэнэ хоёрдмол. Бүтэн жагсаалт: зэрэгцүүлэгчийг үгийн сан дээр ажиллуулж гаргана. Аппад аюулгүй: зэрэгцүүлж чадаагүй үгэнд ふりがな огт харуулахгүй. |
| 9 | Хэрэглээний гүний тоо **хоосон** | хугацаа | `2026-09-11a` хүртэл цугларсан 34 төхөөрөмжийн `cards/answers/learned` нь 0. Эргэж ирсэн хүний л тоо бөглөгдөнө — өмнөх хөдөлмөрийг сэргээх боломжгүй. |

---

## 5. Тогтмол хэрэглэдэг командууд

### Supabase — SQL Editor-т хадгалсан асуултууд

| # | Нэр | Юу хийдэг |
|---|---|---|
| 01 | Схем | `SUPABASE.sql`-ийн бүтэн хуулбар. Давтагдахад тэсвэртэй. |
| 02 | Тестийн өгөгдөл цэвэрлэх | Хөгжүүлэлтийн мөрийг устгана. |
| 03 | Хэрэглээний задаргаа | Цаг тутмын шинэ төхөөрөмж. |
| 04 | Ping шалгалт (rollback) | `ping()`-ийн БИЧИХ замыг шалгана. |

`04` нь регресс тест — `ping()`-ийг өөрчлөх бүрд ажиллуулна. Хүлээгдэх
үр дүн **нэг мөр: `1 · 12 · 34 · 5`**. Энэ мөр гурван зүйлийг нэг дор
нотолно: бичих зам ажиллаж байна · `p_bump:false` нь `opens`-ыг
нэмээгүй (2 биш 1) · `greatest()` нь бага тоог үл тоосон (3/4/1 биш).

`begin … rollback` тул хэдэн ч удаа ажиллуулж болно — хуурамч
төхөөрөмж бүртгэгдэхгүй (§2.13-ын алдаанд дахин орохгүй).

```sql
begin;
select public.ping('zzz-verify-0001-test', 12, 34, 5, true);
select public.ping('zzz-verify-0001-test', 3, 4, 1, false);
select opens, cards, answers, learned
  from public.devices where dev = 'zzz-verify-0001-test';
rollback;
```

**Анхаар:** plpgsql функцийн дотоод SQL нь ҮҮСГЭХ үедээ бүрэн
шалгагддаггүй. Баганын нэр буруу байсан ч функц асуудалгүй үүсээд,
зөвхөн АЖИЛЛАХ үед л унана. Тиймээс `create` амжилттай болсон нь
нотолгоо БИШ — бичих замыг заавал дуудаж үз.

### Локал командууд

```bash
# PDF -> түүхий JSON   (ном: аргументгүй = starter)
python tools/extract_vocab.py            # 入門
python tools/extract_vocab.py el1        # 初級1
python tools/extract_vocab.py el2        # 初級2

# түүхий JSON -> аппын өгөгдөл  (ижил аргумент)
python tools/build_data.py
python tools/build_data.py el1
python tools/build_data.py el2

# N5 (.docx -> JSON).  Эх файл нь репод ОРДОГГҮЙ.
python tools/extract_n5.py "<1-25.docx>" "<26-50.docx>"
python tools/build_n5.py

# ханз (KANJIDIC2 + JLPT + гурван номын хичээлийн тэмдэглэгээ)
python tools/build_kanji.py

# дуу — VOICEVOX хөдөлгүүр ЭХЛЭЭД асаана
tools/_vv/windows-cpu/run.exe --host 127.0.0.1 --port 50021
python tools/build_audio.py --list-speakers
python tools/build_audio.py --speaker 2 --bitrate 96k     # байгааг алгасна
python tools/build_audio.py --speaker 2 --bitrate 96k --force

# тест — гурван ном бүгд. exit 0 = цэвэр
node tools/test_answers.js

# тест — аппын АЖИЛЛАГАА браузер дотор (227 шалгуур). exit 0 = цэвэр
python -m http.server 8765 --bind 127.0.0.1 &    # өөр цонхонд
node tools/test_ui.js

# тест — ДОВТОЛГОО: аппыг зориудаар эвдэх 24 оролдлого. exit 0 = цэвэр
node tools/test_attack.js

# шалгалтын асуултын сан (Irodori 入門, 100 асуулт)
# Асуултыг tools/build_exam.py дотор засаад дахин ажиллуулна.
# ふりがな нь ТЭНД үүсч ТЭНДЭЭ шалгагдана — эргэлзээтэй бол build УНАНА.
# Хариулт/сандруулагч бүр vocab.json-д БАЙХ ёстой — эс тэгвэл унана.
python tools/build_exam.py

# амьд хувилбарыг шалгах
curl -s "https://nova-xo.github.io/irodori-vocab/index.html?n=$RANDOM" | grep -o 'app.js?v=[^"]*'
```

**Репод ОРДОГГҮЙ (`.gitignore`):** `tools/_vv/` (VOICEVOX 1.7 ГБ) ·
`tools/_kd/` (KANJIDIC2) · `tools/_src/` (эх PDF) · `tools/vocab_raw*.json` ·
`tools/report*.txt`

---

## 6. Аудитын түүх

### 2026-09-22 — өргөн хүрээний аудит (UI · engine · дуудлага)

Гурван чиглэлээр, Codex-тэй хамтран.

| Чиглэл | Арга | Үр дүн |
|---|---|---|
| Дуудлага | 6287 үгийн бүтэц + канагийн дууны файл тулгалт | **4 хос ижил дуутай** (байт хүртэл) → `klisten` эвдэрсэн байв |
| UI | 5 утасны хэмжээ × 7 дэлгэц = 35 хослол, автомат | **3 бүлэг олдвор**: `.statsum` халилт, хүрэх талбай, слайдын товч |
| Engine | Codex `gpt-6-astra` | 6 олдвор, **4-ийг баталж зассан**, 1 нь дизайны шийдвэр, 1 нь маргаантай |

**Хамгийн ноцтой:** `checkTyped()` нь **япон гараар зөв бичсэн
хариултыг «буруу» гэж тэмдэглэдэг** байв (§2.48). Латинаар бичсэн
хэрэглэгч л зөв хариулт авч байсан.

Codex-ийн олдворуудаас **ТАТГАЛЗААГҮЙ** боловч засаагүй нэг зүйл:
синкийн уусгалт нь `n` тэнцвэл ИЛҮҮ ӨНДӨР хайрцгийг сонгодог тул
хоёр төхөөрөмж дээр зэрэг давтахад АЛДААНЫ буулт (демоushn) алдагдаж
магадгүй. Энэ нь баримтжуулсан шийдвэр (README «Нийлүүлэх дүрэм») бөгөөд
серверийн SQL-ийг ч хамт өөрчлөх шаардлагатай тул эзэнтэй зөвшилцөх
хэрэгтэй.

### 2026-09-11 — үндсэн бүтцийн бүрэн аудит

Дөрвөн чиглэлээр: ажиллагаа · код · өгөгдөл · аюулгүй байдал.

| Чиглэл | Арга | Үр дүн |
|---|---|---|
| Ажиллагаа | `tools/test_ui.js` — 85 шалгуур | **84 цэвэр · 1 олдвор** (`n-vocab`) |
| Код | Codex `gpt-6-astra` + өөрийн давталт | **21 олдвор**, 6-г нь браузерт давтав |
| Өгөгдөл | `id` давхардал, дутуу талбар, дуу | **2 олдвор** (кана хоосон) |
| Аюулгүй | нууц түлхүүр, `esc()`, RLS | **цэвэр** |

Codex-ийн 21 олдворын 6-г нь браузер дотор бие даан давтаж баталсан
(6/6). Үлдсэнийг нь кодыг уншиж шалгав. Хамгийн ноцтой нь **№4** —
`refreshHome()` нь `pool`-ыг солиод ханзны «давтах» замыг унагадаг
(§2.27).

**Засвар (`2026-09-11b`):** 21 олдвор БҮГД засагдав. Тус бүрд нь
регресс тест бичив (`tools/test_ui.js` §9) — нийт 102 шалгуур, бүгд
цэвэр. Дэлгэрэнгүй нь `CHANGELOG.md`.

### 2026-09-11 — довтолгооны шалгалт (red-team)

`tools/test_attack.js` — 24 довтолгоо дөрвөн хаалгаар: нөөц файл ·
`localStorage` · URL · синкийн сервер. Хатууруулахаас өмнө **10 нь
амжилттай**, дараа нь **0**.

| Эмзэг байдал | Ноцтой | Засвар |
|---|---|---|
| `esc()` атрибутад хашилт хамгаалдаггүй → XSS | 🔴 | `escA()` |
| Хортой нөөц файл аппыг унагана (5 хувилбар) | 🔴 | `cleanSettings` / `cleanProgress` |
| `put_progress` 2000 картад 13.2 сек | 🟠 | set-based уусгалт |
| `add_feedback` спам хязгаар тойрогдоно → Issue үер | 🟠 | нийт 20/10мин |
| `p_meta`, `p_data`, шинэ төхөөрөмж хязгааргүй | 🟠 | 4 КБ / 4 МБ / 200 цагт |

Codex энэ даалгавраас **татгалзсан** (§2.32) — бүхэлд нь гараар хийв.

**Аюулгүй байдал — цэвэр:**
- Репод `sb_secret_`, `service_role`, `ghp_`, JWT ОЛДСОНГҮЙ. Тохирсон
  мөрүүд нь бүгд «үүнийг бүү хий» гэсэн БАРИМТ.
- `innerHTML` 24 газарт. Өгөгдөл орох бүхэн `esc()`-тэй. Атрибут руу
  орох 4 газар нь өөрсдийн тогтмол id (`JO-`, `JK-`, `EX-`, таб код).
- `settings` нь HTML рүү зөвхөн `settings.goal` (тоо) хэлбэрээр ордог.
- Синк код 31^12 ≈ 7.8·10^17 — тааварлах боломжгүй.


| Огноо | Юу шалгасан | Олдсон |
|---|---|---|
| 2026-09-10 | 入門 PDF задлалт | 399 → 28 → **9** асуудал (`Y_TOL` 5→12, `／`-үргэлжлэл) |
| 2026-09-10 | Утга дутуу бичлэг | 6 олдсон, `tools/fixes.json`-оор зассан → 0 |
| 2026-09-10 | Сонголтод япон үг холилдох | `optText` нь утга дутуу үед `jp` рүү ухардаг байсан → сандруулагчийг шүүсэн |
| 2026-09-10 | L0-ийн монгол орчуулга | 14 засвар (`脈` = судас→**судасны цохилт**, `塾`, `瓶`, `ぷんぷん`) |
| 2026-09-10 | L0-ийн үсгийн алдаа | 10 тодруулж зассан (`ろつそく`→`ろうそく` г.м.) |
| 2026-09-10 | `test_answers.js`-ийн хүлээлт | **2 хүлээлт нь өөрөө буруу** байсан: `konnichiwa`→`こんにちは` (жинхэнэ нь `こんにちわ`), `shitsureeshimasu`→`しつれえしまむ` (`しまむ` гэдэг үг ч байхгүй). Код зөв байсан. Хуучин тест DIFF хэвлээд exit 0 буцаадаг тул хэзээ ч анзаарагдаагүй. |
| 2026-09-10 | GitHub Action | Гурван тусдаа алдаа: (1) нууц дахь зай → `curl (3)`, (2) `set -e` + `[…] && exit`, (3) публик түлхүүр RLS-ийг тойрдоггүй тул **чимээгүй** `[]`. Дөрөв дэх нь Issue-ийн гарчиг байтаар таслагдаж эвдэрсэн үсэг үлдээж байсан. |
| 2026-09-10 | `section` талбар | el1/el2-т зөвхөн дугаар («1»), дараа нь хэсэгчилсэн («日») болж байсан → y-ээр нэгтгэж, дугаарыг текстээр хассан → 0 дугаар |
